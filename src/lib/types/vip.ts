export interface VipFormData {
  id?: string;
  venue_id: string;
  event_id: string;
  package_name: string;
  description: string;
  price: number;
  benefits: string[];
  image_url: string;
  is_active: boolean;
}
